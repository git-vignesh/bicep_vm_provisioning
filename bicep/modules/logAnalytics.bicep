// Log Analytics module removed — placeholder retained to avoid breaking imports.
// If you truly want to delete it from the repository, remove this file.

// Log Analytics module removed — placeholder retained to avoid breaking imports.
// If you truly want to delete it from the repository, remove this file.

// No-op: module removed. Provide empty outputs so existing callers won't break.
output workspaceId string = ''
output workspaceKey string = ''
